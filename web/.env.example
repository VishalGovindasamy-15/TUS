# API base URL. Default /api/v1 uses the Vite dev proxy (see vite.config.ts).
# For a production build served separately, set the absolute URL, e.g.:
# VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_API_BASE_URL=/api/v1
